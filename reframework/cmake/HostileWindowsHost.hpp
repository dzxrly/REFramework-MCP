#pragma once

#include <Windows.h>

#ifndef min
#error "The hostile-host compile target requires the Windows min macro"
#endif

#ifndef max
#error "The hostile-host compile target requires the Windows max macro"
#endif
